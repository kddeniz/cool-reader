{
  "name": "cool-reader",
  "private": true,
  "description": "Dev tooling only — the app ships as static HTML/CSS/JS (no runtime npm dependency).",
  "scripts": {
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "lint": "eslint app.js scripts",
    "validate:html": "html-validate index.html docs/api.html",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.50.0",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "html-validate": "^10.13.1",
    "turndown": "^7.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
